# Replit setup

## Run the app

The project runs with Node.js 22 and Vite:

```bash
npm run dev
```

The preview workflow starts the app on port 3000. The app loads without additional configuration; AI features may require a `GEMINI_API_KEY` in the environment.